rich>=13.0.0

# Linux host packages needed: util-linux (lsblk), udev, mount
numpy>=1.24.0
scipy>=1.10.0

# The nanosuit startup voice has optional, heavier dependencies (torch,
# coqui-tts) kept out of this base file so the Docker image stays lean.
# For the exact-copy / neural voice: pip install -r requirements-voice.txt
